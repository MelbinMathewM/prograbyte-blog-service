import { IBaseRepository } from "@/repositories/IBase.repository";
import { IBlogProfile } from "@/models/blog-profile.model";

export interface IBlogProfileRepository extends IBaseRepository<IBlogProfile> {
    
}